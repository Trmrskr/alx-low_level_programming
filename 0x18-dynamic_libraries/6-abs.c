#include "main.h"

/**
 * _abs - function to return absoulute value
 * @n: number argument
 * Return: 0 if successful
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

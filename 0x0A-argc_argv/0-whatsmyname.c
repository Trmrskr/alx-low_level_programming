#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of main argument
 * @argv: array vector
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}

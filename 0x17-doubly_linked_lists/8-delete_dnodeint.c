#include "lists.h"

/**
 * delete_dnodeint_at_index - delete node at an index
 * @head: head of node to be deleteed
 * @index: the index to be deleted rom
 * Return: 1, or -1
 */
int delete_dnodeint_at_index(dlistint_t **head, unsigned int index){
	return (1);
}
